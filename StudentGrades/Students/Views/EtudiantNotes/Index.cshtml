@model IEnumerable<Students.Models.EtudiantNote>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Etudiant.Nom)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Matiere.Nom)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Note.Type)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Valeur)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Etudiant.Nom)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Matiere.Nom)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Note.Type)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Valeur)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.EtudiantNoteId }) |
            @Html.ActionLink("Details", "Details", new { id=item.EtudiantNoteId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.EtudiantNoteId })
        </td>
    </tr>
}

</table>
